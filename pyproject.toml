[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ptev2"
version = "0.0.0"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff.lint]
ignore = ["F841"]
