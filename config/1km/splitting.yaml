data_dir: "/scratch3/plant-traits-v2/data/"
validity_mask: "${data_dir}1km/validity_mask.tif"

targets:
  resolution_km: 1

h3:
  resolution: 2

splitting:
  train_frac: 0.70
  val_frac: 0.20
  test_frac: 0.10

jsd:
  bands: [1]
  n_restarts: 200
  n_bins: 50
  random_seed: 42

debug:
  n_traits: null  # set to an integer to limit to first N traits (e.g. 5 for quick testing)
