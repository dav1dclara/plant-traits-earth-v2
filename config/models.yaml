defaults:
  - config
  - _self_

# Backward-compatible aliases for older scripts that still read models.yaml directly.
seed: ${training.seed}
device: ${training.device}
model_name: ${models.name}
model: ${models.active}
optim:
  lr: ${training.optimizer.lr}
  weight_decay: ${training.optimizer.weight_decay}
