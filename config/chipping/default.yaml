# Configuration file for chipping process
# Currently fixed to 22km data and trait X1080 -> # TODO: make more flexible

paths:
  predictors_dir: /scratch3/plant-traits-v2/data/22km/eo_data_processed/
  targets_dir: /scratch3/plant-traits-v2/data/22km/
  splits_file: /scratch3/plant-traits-v2/data/temp/h3_splits/h3_res1_X1080_mean.gpkg
  zarr_dir: /scratch3/plant-traits-v2/data/chips/22km/

settings:
  patch_size: 15
  stride: 10

data:
  root_dir: /scratch3/plant-traits-v2/data/22km/
  predictors_dir: /scratch3/plant-traits-v2/data/22km/eo_data_processed/
  predictors:
    canopy_height:
      use: true
      bands: []
    modis:
      use: true
      bands: []
    soil_grids:  # TODO: rename back to soilgrids
      use: true
      bands: []
    vodca:
      use: true
      bands: []
    worldclim:
      use: true
      bands: []

  targets:
    gbif:
      use: true
      traits: [1080]
    splot:
      use: false
      traits: [1080]
