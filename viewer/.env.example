# Public URL of the predictor PMTiles file on Cloudflare R2 (must allow HTTP range requests).
# Used in the map source as: pmtiles://$VITE_PMTILES_URL
# Leave empty to load just the bare globe.
VITE_PMTILES_URL=
