# Public URL of the DEM PMTiles file on Cloudflare R2 (must allow HTTP range requests).
# Used in the map source as: pmtiles://$VITE_DEM_PMTILES_URL
# Leave empty to load just the bare globe.
VITE_DEM_PMTILES_URL=
